import React, { FunctionComponent } from 'react';
import RoleTypes from './Role.View.Types';

const Role: FunctionComponent<RoleTypes> = ({location}) => {
    console.log(location);
    return (
        <div>
            
        </div>
    )
}

export default Role;